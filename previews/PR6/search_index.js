var documenterSearchIndex = {"docs":
[{"category":"section","location":"debugging/#Debugging","page":"Debugging","text":"Lava exposes a small set of introspection and recovery utilities for development and post-mortem.","title":"Debugging"},{"category":"section","location":"debugging/#Inspect-the-device","page":"Debugging","text":"ctx = Lava.vk_context()\n@show ctx.device_name\n@show ctx.queue_family\n@show ctx.ray_query_available\n@show ctx.rt_pipeline_properties","title":"Inspect the device"},{"category":"section","location":"debugging/#Memory-usage","page":"Debugging","text":"gpu_memory_usage()\n# → (live_bytes = 1.2e9, live_buffers = 14, default_bq = (...) , ...)\n\nReturns a snapshot of currently-live device buffers, mapped memory, slab pools, and per-batch refs. Useful to spot leaks across iterations.","title":"Memory usage"},{"category":"section","location":"debugging/#Full-runtime-state-dump","page":"Debugging","text":"dump_state()\n\nPrints batch queue contents, in-flight command buffers, deferred frees, slab inventories, and the timeline semaphore counters. The same information backs the mwe_*.jl regression tests in test/.","title":"Full runtime state dump"},{"category":"section","location":"debugging/#Dispatch-logging","page":"Debugging","text":"set_dispatch_logging!(true)\n# ... run your code ...\nlog = get_dispatch_log()   # Vector of (kernel_name, dispatch_time_µs, args_summary)\n\nThis is how the per-dispatch latency numbers in Performance were measured.","title":"Dispatch logging"},{"category":"section","location":"debugging/#Recovering-from-device-lost","page":"Debugging","text":"A driver bug, OOM, or your own kernel writing past a buffer can leave the Vulkan device in a lost state. Recover without restarting Julia:\n\nLava.vk_reset_device!()\n\nThis tears down the current VkDevice (and everything dependent on it) and creates a fresh one. Pre-existing LavaArrays become invalid — recreate them after the reset.","title":"Recovering from device-lost"},{"category":"section","location":"debugging/#Where-to-look-for-breadcrumbs","page":"Debugging","text":"Validation layers — Lava enables VK_LAYER_KHRONOS_validation by default in debug builds; messages are printed and also captured in LavaError. Disable with LAVA_NO_VALIDATION=1 for production.\nLast compiled IR and SPIR-V — every kernel compile writes the post-pass LLVM IR to /tmp/lava_last.ll and the resulting SPIR-V to /tmp/lava_last.spv. The disassembly is at /tmp/lava_last.dis. After a spirv-val failure these are the artefacts to inspect.\nPer-kernel dumps — dev/Lava/tmp_kernels/kernel_<n>_<entryname>.{ll,spv} is written for every successful compile, so you can diff between two kernels or two Julia runs.\nGitHub Actions logs — the CI runs the full test suite under lavapipe and is the easiest place to see what a clean run looks like.","title":"Where to look for breadcrumbs"},{"category":"section","location":"debugging/#Common-failure-modes","page":"Debugging","text":"Symptom Likely cause Fix\nOpAccessChain into a structure must be an OpConstant A struct member index was emitted via a runtime path Open an issue with /tmp/lava_last.ll and the kernel\nvkCmdTraceRaysIndirectKHR aborts with VK_ERROR_DEVICE_LOST after many compute dispatches on RADV Known driver bug, see Known Issues Workaround listed there\nglfwInit failed on package load Headless box without DISPLAY Run under xvfb-run or set LAVA_HEADLESS=1 (compute only)\nTest suite fails with display errors Same as above xvfb-run -s '-screen 0 1024x768x24' julia --project -e 'using Pkg; Pkg.test()'\nMethodError from broadcasting between LavaArray and Array Implicit host-device mixing Use Adapt.adapt(LavaBackend(), array) explicitly","title":"Common failure modes"},{"category":"section","location":"compute/#Compute","page":"Compute","text":"Lava implements the standard Julia GPU contract: LavaBackend <: KernelAbstractions.GPU and LavaArray{T,N} <: GPUArrays.AbstractGPUArray. Code written against those interfaces — your own kernels, GPUArrays.jl operations, AcceleratedKernels.jl — runs on Vulkan without changes.","title":"Compute"},{"category":"section","location":"compute/#LavaBackend-and-LavaArray","page":"Compute","text":"using Lava, KernelAbstractions\n\nbackend = LavaBackend()\n\na = LavaArray(rand(Float32, 1024))\nb = LavaArray(rand(Float32, 1024))\nc = LavaArray{Float32}(undef, 1024)\n\n@kernel function vadd!(C, A, B)\n    i = @index(Global)\n    C[i] = A[i] + B[i]\nend\n\nvadd!(backend)(c, a, b; ndrange=length(c))\n@assert Array(c) ≈ Array(a) .+ Array(b)\n\nMost KA features work out of the box:\n\nKA feature Status\n@index(Global) / @index(Local) / @index(Group) ✓\n@groupsize / @uniform / @private ✓\n@localmem with primitive and struct element types ✓\n@synchronize (workgroup barrier) ✓\nConst reads ✓\nDynamic ndrange and workgroup size ✓\nHeterogeneous launch (mixed-type args) ✓","title":"LavaBackend and LavaArray"},{"category":"section","location":"compute/#Broadcasting-and-reductions","page":"Compute","text":"LavaArray participates in regular Julia broadcasting and mapreduce. Both go through the same SPIR-V compilation path, so a one-liner like\n\nv = LavaArray(rand(Float32, 10^7))\nsum(sin, v) / length(v)\n\ncompiles two kernels (the elementwise sin and a tree reduction) and dispatches them on the GPU.","title":"Broadcasting and reductions"},{"category":"section","location":"compute/#Atomics","page":"Compute","text":"Lava supports atomic operations through Atomix.jl on the standard 32-bit integer and float types:\n\nusing Atomix\n\n@kernel function atomic_hist!(counts, samples)\n    i = @index(Global)\n    @inbounds bucket = clamp(samples[i] >> 4, 0, length(counts) - 1) + 1\n    Atomix.@atomic counts[bucket] += 1\nend\n\nSupported ops: + - & | ⊻ xchg min max on Int32/UInt32, plus add/min/max on Float32 (implemented via a CAS loop where the hardware doesn't have native float atomics). cmpxchg! is also supported.","title":"Atomics"},{"category":"section","location":"compute/#Buffer-Device-Address-(BDA)-arguments","page":"Compute","text":"All kernel arguments are packed into a single device-memory buffer and passed via a single 8-byte push constant pointing to the BDA. Consequences:\n\nNo 256-byte push-constant limit — argument count and total size are practically unbounded.\nNo descriptor sets per buffer — LavaArray arguments become BDA pointers automatically.\nisbits structs are passed by value, copied into the arg buffer.\nLavaArray/LavaDeviceArray arguments are passed by raw GPU address.\n\nFor the curious: src/compiler/entry_wrapper.jl wraps each kernel entry so its first argument is the BDA, and the wrapper unpacks the original arguments before calling the user function.","title":"Buffer Device Address (BDA) arguments"},{"category":"section","location":"compute/#Memory-model","page":"Compute","text":"Three memory regions are visible to compute kernels:\n\nLavaArray (device-local) — the default. Lives on the GPU; copied to/from host via Array(...)/copyto!.\nUnified / BAR memory — auto-selected for small buffers (≤ 64 bytes by default). Host has a directly mapped pointer, so Array(buf) is a single fence instead of a fence + staging copy.\n@localmem — workgroup-scoped scratchpad. Supports primitive types, primitive arrays, and isbits struct arrays.","title":"Memory model"},{"category":"section","location":"compute/#Compilation-cache","page":"Compute","text":"Compiled SPIR-V is cached at two layers:\n\nIn-memory by Julia MethodInstance (zero-cost after first compile)\nOn-disk under ~/.julia/compiled/Lava/spirv/..., keyed by IR hash + Vulkan device fingerprint\n\nTo force a recompile after editing a kernel:\n\nLava.clear_kernel_cache!()\n\nTo wipe the on-disk cache too (rarely needed):\n\nLava.clear_spirv_disk_cache!()","title":"Compilation cache"},{"category":"section","location":"compute/#Performance-notes","page":"Compute","text":"Per-dispatch overhead is ~1.8 µs (vs ~8.8 µs for AMDGPU). At small problem sizes this is what dominates.\nMultiple dispatches are batched into a single command buffer. They flush automatically before a host read (Array(buf)); call vk_flush!(backend.bq) to flush explicitly.\nFor tight CPU↔GPU loops, prefer Unified/BAR memory by allocating small buffers explicitly through KA's unified=true allocator.\n\nSee Performance for benchmarks and tuning tips.","title":"Performance notes"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"This page lists the public surface of Lava. Items marked stable belong to the GPUArrays / KernelAbstractions interface and follow semver. Graphics and ray-tracing items are still evolving.","title":"API Reference"},{"category":"section","location":"api/#Backend-and-arrays","page":"API Reference","text":"","title":"Backend & arrays"},{"category":"section","location":"api/#Compute-kernels","page":"API Reference","text":"","title":"Compute kernels"},{"category":"section","location":"api/#Hardware-ray-tracing","page":"API Reference","text":"","title":"Hardware ray tracing"},{"category":"section","location":"api/#Graphics","page":"API Reference","text":"","title":"Graphics"},{"category":"section","location":"api/#Debugging","page":"API Reference","text":"","title":"Debugging"},{"category":"section","location":"api/#Index","page":"API Reference","text":"","title":"Index"},{"category":"type","location":"api/#Lava.LavaBackend","page":"API Reference","text":"LavaBackend <: KA.GPU\n\nLava's GPU compute backend. Carries the Vulkan context and batch queues explicitly.\n\ndispatch_bq: where KA kernel dispatches are recorded.\nupload_bq:   where CPU→GPU transfers (upload!, download!, staging)                record their copies. May be the same queue as dispatch_bq                (single-queue mode) or a separate async queue for true                upload/compute overlap.\nLavaBackend()                      # default: dispatch + upload both on defaultbq LavaBackend(bq)                    # single queue for both LavaBackend(dispatchbq, upload_bq)  # split — enables pipelining\n\nLavaBackend() with no arguments resolves dispatch_bq / upload_bq lazily via vk_context().default_bq at every property access.  Pinning would break after vk_reset_device!(): a const BACKEND = LavaBackend() created at module-load would keep a stale BatchQueue tied to the old VkDevice, and every subsequent buffer created via that backend would end up allocated on the dead device — later triggering VUID-vkCmdCopyBuffer-commonparent and a page-aligned GPUVM fault. Explicit queues passed to LavaBackend(bq) / LavaBackend(d, u) are pinned on purpose (the caller wants those exact queues, e.g. an async upload queue).\n\n\n\n\n\n","title":"Lava.LavaBackend"},{"category":"type","location":"api/#Lava.LavaArray","page":"API Reference","text":"LavaArray{T,N} <: AbstractGPUArray{T,N}\n\nGPU array backed by a Vulkan device-local buffer with BDA (Buffer Device Address).\n\n\n\n\n\n","title":"Lava.LavaArray"},{"category":"type","location":"api/#Lava.LavaDeviceArray","page":"API Reference","text":"LavaDeviceArray{T,N}\n\nDevice-side isbits array representation for GPU kernels. Contains a Ptr{T} (actually a BDA address) and dimensions.\n\n\n\n\n\n","title":"Lava.LavaDeviceArray"},{"category":"function","location":"api/#Lava.lava_launch!","page":"API Reference","text":"lava_launch!(bq, f, args...; ndrange, workgroup_size=(64,1,1))\n\nCompile and dispatch a Julia function as a Vulkan compute kernel on bq.\n\nArguments flow through LavaAdaptor(batch), which strips every LavaArray reached by Adapt.adapt (top level or nested inside closures / wrapper structs) to a LavaDeviceArray{T,N} AND pins its backing VkManagedBuffer into the batch.  Kernels therefore see exactly the post-adapt types — the same ABI as KernelAbstractions — never Ptr{T} for what was originally a LavaArray.\n\nExample:     a = LavaArray{Float32,1}(undef, (n,))     lavalaunch!(bq, mykernel, a, b, Int32(n); ndrange=n, workgroupsize=(256,1,1))     # kernel signature: mykernel(a::LavaDeviceArray{Float32,1}, ...)\n\n\n\n\n\n","title":"Lava.lava_launch!"},{"category":"function","location":"api/#Lava.vk_flush!","page":"API Reference","text":"vk_flush!(bq::BatchQueue)\nvk_flush!(ctx::VkContext)       # flushes ctx.default_bq\n\nFlush a specific batch queue.  Always spell the queue (or its ctx) explicitly — zero-arg convenience forms have been removed per the \"Explicit arguments over implicit state\" rule.\n\n\n\n\n\n","title":"Lava.vk_flush!"},{"category":"function","location":"api/#Lava.clear_kernel_cache!","page":"API Reference","text":"clear_kernel_cache!()\n\nEvict the in-session kernel + pipeline cache (LINKED_KERNEL_CACHE) so the next dispatch of each kernel recompiles from Julia source.\n\nUse this after editing a Julia kernel under Revise — Revise invalidates the Julia method, but Lava's hash-keyed kernel cache stays populated with the old SPIR-V because hash(f, tt, workgroup_size) doesn't change when the method body changes. Unlike vk_reset_device!(), this keeps all existing LavaArrays and the Vulkan context alive.\n\n\n\n\n\n","title":"Lava.clear_kernel_cache!"},{"category":"function","location":"api/#Lava.clear_spirv_disk_cache!","page":"API Reference","text":"Clear Lava's SPIR-V disk cache.\n\n\n\n\n\n","title":"Lava.clear_spirv_disk_cache!"},{"category":"type","location":"api/#Lava.HWTLAS","page":"API Reference","text":"HWTLAS{Tri} <: Raycore.AbstractAccel\n\nLava-native hardware-accelerated TLAS.  Concretely typed on Tri (the per-primitive triangle type, typically Raycore.Triangle{UInt32}).\n\nBuild geometry with push!(hwtlas, mesh, transform), then call Raycore.sync!(hwtlas) to upload and build the Vulkan AS.  The adapted form lives in hwtlas.static_tlas as a HWAdaptedAccel{HWTLAS{Tri}}.\n\nMutation contract\n\nupdate_transform! / update_transforms! write directly to the batch's GPU-resident instance_buf via a compute kernel and flag transforms_dirty.  The next sync! decides between full rebuild (topology change, dirty=true) and MODE_UPDATE_KHR refit (transforms_dirty=true).  No CPU-side staging.\n\nAdapted-form invariant\n\nsync!(hwtlas) is the sole owner of hwtlas.static_tlas.  It rebuilds as efficiently as possible — in place via resize!/copyto! where the backing buffer can be reused, reallocated only when a buffer grew — and stores the result in hwtlas.static_tlas.  sync! MAY reassign hwtlas.static_tlas when a buffer was reallocated.\n\nEvery consumer that hands the accel to a raytracing dispatch MUST go through hwtlas.static_tlas or Adapt.adapt(backend, hwtlas) (which reads / refreshes hwtlas.static_tlas) per dispatch.  Both are cheap; sync! did the heavy lifting.  NEVER cache the HWAdaptedAccel returned by adapt across mutations — consumers that cache silently see stale geometry.\n\nNon-blocking sync!\n\nsync!(hwtlas) does NOT call KA.synchronize(backend).  Old backings are dropped via Lava.unsafe_free!, which defers destruction through hwtlas.bq's timeline (bq.deferred_as_frees / bq.deferred_frees) when prior dispatches are still in flight.  Phase-B pinning of RT closure leaves (trigpu, offgpu, hwtlas, hwaccel) is what makes the timeline tracking correct on the BDA path.\n\nFor a CPU-blocking drain use Raycore.wait_for_gpu!(hwtlas), which calls vk_flush!(hwtlas.bq) (waits on the HWTLAS's own queue specifically, not the backend-wide queue the Raycore default wait_for_gpu! uses).\n\n\n\n\n\n","title":"Lava.HWTLAS"},{"category":"type","location":"api/#Lava.HardwareAccel","page":"API Reference","text":"HardwareAccel{TriVec <: AbstractVector}\n\nHardware-accelerated ray tracing context. Built from a Raycore-compatible TLAS. Parametrised on the concrete triangle-vector type (e.g. Vector{Raycore.Triangle{UInt32}}) so field accesses stay type-stable.\n\nFields\n\ntlas::LavaTLAS — Vulkan top-level acceleration structure\ntriangle_data::TriVec — CPU vector of all primitives (for lookup after trace)\nblas_offsets::Vector{UInt32} — Per-BLAS offset into triangle_data\nrt_pipeline::RayTracingPipeline — Pre-compiled raygen+closesthit+miss\n\nUsage\n\nhw = HardwareAccel(raycore_tlas)\nresults = LavaArray{RTHitResult}(n_rays)\ntrace_closest_hits!(results, rays, hw)\n\n\n\n\n\n","title":"Lava.HardwareAccel"},{"category":"type","location":"api/#Lava.RayTracingPipeline","page":"API Reference","text":"RayTracingPipeline\n\nA ray tracing pipeline defined by Julia functions for each shader stage. Shaders are compiled lazily on first trace_rays! call and cached.\n\nConstructor\n\nRayTracingPipeline(; raygen, closest_hit, miss, payload_type=:f32)\n\nExample\n\nfunction my_raygen(output::LavaDeviceArray{Float32,1})\n    lid = Lava.lava_rt_launch_id_x()\n    Lava.lava_rt_payload_store_f32(-1f0)\n    Lava.lava_rt_trace_ray(...)\n    t = Lava.lava_rt_payload_load_f32()\n    output[lid + 1] = t\nend\nfunction my_chit()\n    Lava.lava_rt_payload_store_f32(Lava.lava_rt_ray_tmax())\nend\nfunction my_miss()\n    Lava.lava_rt_payload_store_f32(-1f0)\nend\n\nrt = RayTracingPipeline(raygen=my_raygen, closest_hit=my_chit, miss=my_miss)\ntrace_rays!(rt, tlas, output_buf; width=1920, height=1080)\n\n\n\n\n\n","title":"Lava.RayTracingPipeline"},{"category":"function","location":"api/#Lava.trace_closest_hits!","page":"API Reference","text":"trace_closest_hits!(results, rays, accel::HardwareAccel, n_rays::Integer;\n                    cull_mask::UInt32 = UInt32(0xFF))\n\nTrace n_rays rays against the hardware acceleration structure. Results are written to results buffer (one RTHitResult per ray).\n\nresults and rays can be LavaArray{RTHitResult}/LavaArray{RTRay}, or any type accepted by trace_rays!.\n\ncull_mask is ANDed against each instance's instancemask in the TLAS. An instance is visible to a ray only when `(cullmask & instance_mask) != 0. Default0xFF` matches all instances (backward-compatible).\n\n\n\n\n\n","title":"Lava.trace_closest_hits!"},{"category":"type","location":"api/#Lava.GraphicsPipeline","page":"API Reference","text":"GraphicsPipeline\n\nHigh-level graphics pipeline wrapping Julia shader functions. Compiles lazily on first use and caches the result.\n\nFields\n\nvertex, fragment: Required Julia shader functions\ngeometry: Optional (func, GeometryConfig) tuple\ntess_control, tess_eval: Optional tessellation stages\nblend, cull, topology, depth: Pipeline state types\n\n\n\n\n\n","title":"Lava.GraphicsPipeline"},{"category":"type","location":"api/#Lava.RenderWindow","page":"API Reference","text":"RenderWindow\n\nA window with Vulkan surface and swapchain for presenting rendered frames. Uses GLFW for cross-platform window management.\n\n\n\n\n\n","title":"Lava.RenderWindow"},{"category":"type","location":"api/#Lava.LavaFramebuffer","page":"API Reference","text":"LavaFramebuffer\n\nOffscreen render target with color and optional depth images. Used for render-to-texture or offscreen rendering.\n\n\n\n\n\n","title":"Lava.LavaFramebuffer"},{"category":"type","location":"api/#Lava.LavaTexture2D","page":"API Reference","text":"2D texture backed by VkImage.\n\n\n\n\n\n","title":"Lava.LavaTexture2D"},{"category":"type","location":"api/#Lava.LavaSampler","page":"API Reference","text":"Reusable sampler configuration.\n\n\n\n\n\n","title":"Lava.LavaSampler"},{"category":"function","location":"api/#Lava.present_frame!","page":"API Reference","text":"present_frame!(bq::BatchQueue, win::RenderWindow)\n\nSubmit recorded draw commands and present to screen.\n\n\n\n\n\n","title":"Lava.present_frame!"},{"category":"function","location":"api/#Lava.vk_context","page":"API Reference","text":"vk_context() -> VkContext\n\nGet or create the global Vulkan context. Lazily initializes on first call.\n\n\n\n\n\n","title":"Lava.vk_context"},{"category":"function","location":"api/#Lava.vk_reset_device!","page":"API Reference","text":"vk_reset_device!()\n\nReinitialize the Vulkan device after DEVICE_LOST or other unrecoverable errors. Destroys the old context and creates a fresh one. Clears all caches (pipelines, kernels, arg buffers).\n\nWARNING: All existing LavaArrays become INVALID after reset — their backing GPU buffers no longer exist. You must reallocate all GPU data.\n\n\n\n\n\n","title":"Lava.vk_reset_device!"},{"category":"function","location":"api/#Lava.dump_state","page":"API Reference","text":"dump_state(; io::IO=stdout)\n\nPrint a comprehensive summary of Lava.jl runtime state for debugging.\n\n\n\n\n\n","title":"Lava.dump_state"},{"category":"function","location":"api/#Lava.gpu_memory_usage","page":"API Reference","text":"gpu_memory_usage() -> NamedTuple\n\nReturn current GPU memory usage statistics.\n\n\n\n\n\n","title":"Lava.gpu_memory_usage"},{"category":"function","location":"api/#Lava.set_dispatch_logging!","page":"API Reference","text":"set_dispatch_logging!(enabled::Bool)\n\nEnable or disable dispatch name logging. When enabled, each dispatch records its kernel name and parameters for crash debugging. Disabled by default for zero-alloc performance. Auto-enabled on DEVICE_LOST.\n\n\n\n\n\n","title":"Lava.set_dispatch_logging!"},{"category":"function","location":"api/#Lava.get_dispatch_log","page":"API Reference","text":"get_dispatch_log() -> Vector{String}\n\nReturn a copy of the recent dispatch log (up to 2000 entries).\n\n\n\n\n\n","title":"Lava.get_dispatch_log"},{"category":"section","location":"performance/#Performance","page":"Performance","text":"Numbers below are on AMD RX 7900 XTX / Ryzen 9 7900X unless otherwise noted. The benchmark scripts live in RayDemo/benchmark and the corresponding compute benchmarks under Lava/benchmarks/run_benchmarks.jl.","title":"Performance"},{"category":"section","location":"performance/#Render-benchmarks","page":"Performance","text":"Rendering uses Hikari's wavefront volumetric path tracer via RayMakie.\n\n(Image: RayDemo benchmarks)\n\nHeadline:\n\n Lava SW vs AMDGPU SW Lava HW vs Lava SW Lava SW vs pbrt-v4 (24 CPU threads)\nRender speedup 1.4 – 2.5× 1.0 – 2.3× (geometry-heavy) 5 – 32×\n\nHardware ray tracing adds the biggest wins on geometry-dense scenes (Crown, Killeroo Gold). Software-BVH and HW-RT match within ~3 % per-pixel mean and verify bit-exact against pbrt-v4 on the same scene description.","title":"Render benchmarks"},{"category":"section","location":"performance/#Compute-benchmarks-(AcceleratedKernels.jl)","page":"Performance","text":"(Image: AK 100M)\n\n(Image: AK 10M)\n\nLava wins on compute-bound and dispatch-sensitive operations — up to 23× faster on map(sin, …) at 10M elements.\nAMDGPU wins on memory-bound sort/sortperm where its native HIP path has the edge.","title":"Compute benchmarks (AcceleratedKernels.jl)"},{"category":"section","location":"performance/#What-makes-Lava-fast","page":"Performance","text":"Most of the wins come from removing per-dispatch overhead:\n\n@generated argument packing. All kernel arguments are written directly into a mapped GPU memory buffer in a single generated function. No Any[] boxing, no InlineStructArg vectors, no zeros(UInt8, N) intermediate buffer.\nPipeline cache keyed by IR hash. First dispatch builds the VkPipeline, subsequent ones reuse it.\nBuffer Device Address. Kernel args travel through one 8-byte push constant pointing at the BDA. No per-buffer descriptor sets.\nCompile-time ghost-type elision. Zero-size types (e.g. KA context handles) never reach the GPU.\nBatched command buffers. Multiple dispatches record into one VkCommandBuffer and submit together. Auto-flush on host reads keeps semantics simple.\nUnified/BAR memory for small buffers. ≤ 64-byte buffers (typical for KA workqueue counters) use host-visible device memory, so Array(buf) is one fence instead of one fence + one staging copy.\n\nPer-dispatch recording is 1.8 µs end-to-end (vs ~8.8 µs in AMDGPU), and the full dispatch path including submission is 27 µs when batched.","title":"What makes Lava fast"},{"category":"section","location":"performance/#Tuning-your-own-code","page":"Performance","text":"For latency-sensitive code:\n\nAvoid forcing host↔device sync. Array(big_buffer) triggers a flush.\nRe-use kernels — first compile of a @kernel is slow (a few hundred ms), subsequent dispatches are cached.\nUse Unified/BAR memory for tiny ping-pong buffers between CPU and GPU.\n\nFor throughput-sensitive code:\n\nMake sure the kernel is fat enough that the per-dispatch 27 µs is amortised. At 10M Float32 adds the kernel itself runs in a fraction of a millisecond — overhead dominates if you also do 10 such kernels sequentially.\nUse Atomix.@atomic rather than ad-hoc CAS loops where supported.\nProfile with set_dispatch_logging!(true) — see Debugging.","title":"Tuning your own code"},{"category":"section","location":"known_issues/#Known-Issues","page":"Known Issues","text":"This page mirrors the canonical KNOWN_ISSUES.md at the repo root. Open issues are tracked there with reproducers.\n\nusing Markdown\ncandidates = (\n    joinpath(@__DIR__, \"..\", \"..\", \"KNOWN_ISSUES.md\"),\n    joinpath(@__DIR__, \"..\", \"KNOWN_ISSUES.md\"),\n)\npath = nothing\nfor c in candidates\n    if isfile(c)\n        path = c; break\n    end\nend\npath === nothing ?\n    Markdown.parse(\"`KNOWN_ISSUES.md` could not be located.\") :\n    Markdown.parse(read(path, String))","title":"Known Issues"},{"category":"section","location":"architecture/#Architecture","page":"Architecture","text":"","title":"Architecture"},{"category":"section","location":"architecture/#The-pipeline","page":"Architecture","text":"Julia function\n  │  GPUCompiler.jl\nLLVM IR (via LLVM.jl, LLVM 18)\n  │  Custom LLVM passes:\n  │   ├─ lift_geps        : byte-offset GEPs → typed GEPs (3 passes)\n  │   ├─ structurize_cfg  : CFG fix-up + LLVM StructurizeCFGPass\n  │   ├─ prepare_vulkan   : 8 Vulkan-specific transforms, shared-mem flattening\n  │   └─ lower_intrinsics : math / barrier / thread-index lowering\n  │  Custom SPIR-V emitter (~40 LLVM opcodes → SPIR-V)\n  │  spirv-val validation\nVkShaderModule → VkPipeline → GPU dispatch\n\nThe custom emitter is the architectural choice that makes the rest possible. Roughly 6,000 lines of Julia translate the post-pass LLVM module into SPIR-V directly via the LLVM.jl reflection API.","title":"The pipeline"},{"category":"section","location":"architecture/#Why-not-llc?","page":"Architecture","text":"The LLVM project ships an SPIR-V backend (llc -mtriple=spirv...). Lava does not use it. Empirically (as of LLVM 21):\n\nGeometry shaders crash llc during instruction selection. No upstream fix is planned.\nRay tracing extensions are rejected — OpTraceRayKHR and friends never made it past the prototype stage.\nKey Vulkan extensions are missing patterns: SPV_KHR_physical_storage_buffer (BDA) and SPV_KHR_variable_pointers are both incomplete. These are the features that make Lava's argument-passing model work.\n\nThe custom emitter handles all five graphics stages plus all RT stages from day one. llc output is still used as an oracle for cross-checking compute and vertex/fragment validation in the test suite.","title":"Why not llc?"},{"category":"section","location":"architecture/#Source-layout","page":"Architecture","text":"src/\n├── Lava.jl                          # Module entrypoint, exports, debugging API\n├── compiler/\n│   ├── compilation.jl               # Pipeline orchestration\n│   ├── target.jl                    # GPUCompiler target definition\n│   ├── entry_wrapper.jl             # BDA argument buffer wrapping\n│   ├── passes/\n│   │   ├── lift_geps.jl             # Byte-offset GEP to typed GEP (3 passes)\n│   │   ├── structurize_cfg.jl       # CFG fixup + StructurizeCFGPass\n│   │   ├── prepare_vulkan.jl        # 8 Vulkan IR transforms\n│   │   └── lower_intrinsics.jl      # Math / barrier / thread index\n│   └── spirv/\n│       ├── module.jl                # SPIR-V module builder, ID allocation\n│       ├── types.jl                 # LLVM ↔ SPIR-V type mapping\n│       ├── emit.jl                  # Core emitter (~40 opcodes)\n│       ├── graphics.jl              # Graphics shader entry points\n│       └── raytracing.jl            # RT stages, OpTraceRayKHR\n├── device/\n│   ├── math.jl                      # Float32 GLSL.std.450 + Float64 fallbacks\n│   ├── quirks.jl                    # @device_override for GPU-safe replacements\n│   ├── atomics.jl                   # Atomix.jl integration, CAS loops\n│   ├── rt_intrinsics.jl             # RT builtins\n│   └── gfx_intrinsics.jl            # Graphics builtins\n├── runtime/\n│   ├── device.jl                    # Vulkan device/queue creation, feature detection\n│   ├── memory.jl                    # Buffer allocation, BDA, staging, mapped memory\n│   ├── command.jl                   # Command buffer pool, batched submission\n│   ├── launch.jl                    # lava_launch!, arg buffer pool, vk_flush!\n│   ├── pipeline.jl                  # VkShaderModule + pipeline caching\n│   └── errors.jl                    # LavaError, validation layer capture\n├── array/\n│   ├── lavaarray.jl                 # LavaArray{T,N} + LavaDeviceArray\n│   ├── gpuarrays.jl                 # GPUArrays.jl interface, BroadcastStyle\n│   ├── ka_backend.jl                # LavaBackend, @kernel / @index / @localmem\n│   └── mapreduce.jl                 # GPU mapreducedim! with subgroup reductions\n├── graphics/\n│   ├── api.jl                       # GraphicsPipeline, draw!, blit!, present_frame!\n│   ├── pipeline.jl                  # CompiledGraphicsPipeline\n│   ├── framebuffer.jl               # LavaFramebuffer\n│   ├── window.jl                    # RenderWindow, swapchain, GLFW\n│   └── textures.jl                  # LavaTexture2D, LavaSampler\n└── raytracing/\n    ├── acceleration.jl              # BLAS/TLAS build, HardwareAccel, HWTLAS\n    ├── pipeline.jl                  # RT pipeline, SBT, vkCmdTraceRaysKHR\n    ├── shaders.jl                   # RayTracingPipeline high-level API\n    └── raycore_compat.jl            # trace_closest_hits!, Hikari integration","title":"Source layout"},{"category":"section","location":"architecture/#Buffer-Device-Address-(BDA)","page":"Architecture","text":"The argument-passing model that lets Lava avoid the 256-byte push-constant limit and per-buffer descriptor sets. Every kernel entry is wrapped at compile time to expect one i64 push constant — the BDA of an arg buffer. The wrapper unpacks the original arguments before calling the user function.\n\nThe arg buffer is allocated from a pool of mapped device-memory slabs, reused across dispatches, and replenished as needed. vk_flush! waits for outstanding command buffers and returns slabs to the pool.","title":"Buffer Device Address (BDA)"},{"category":"section","location":"architecture/#Memory-and-command-buffer-lifecycle","page":"Architecture","text":"LavaArray is a thin wrapper around a GPUArrays.DataRef{VkManagedBuffer} plus an offset. The data ref handles reference counting; the underlying VkBuffer is freed when the last view drops and the last command buffer using it has retired.\nCommandBatch pins references to all GPU objects used during recording. Submission moves the batch into bq.in_flight. When the timeline semaphore signals completion, the batch is retired and its refs are released.\nCross-queue synchronisation goes through timeline semaphores — no fence chains, no per-resource owner tracking.","title":"Memory and command-buffer lifecycle"},{"category":"section","location":"architecture/#Compilation-cache","page":"Architecture","text":"Two layers, both transparent to the user:\n\nIn-memory by MethodInstance — zero-cost after the first compile.\nOn-disk under ~/.julia/compiled/Lava/spirv/... — keyed by IR hash × Vulkan device fingerprint. Survives Julia restarts.\n\nLava.clear_kernel_cache!() evicts the in-memory layer; Lava.clear_spirv_disk_cache!() evicts the disk layer.","title":"Compilation cache"},{"category":"section","location":"architecture/#Cross-platform-notes","page":"Architecture","text":"NVIDIA (RTX 30xx, 40xx) — fully supported, including hardware RT.\nAMD RADV (RX 7900, Radeon 8060S) — fully supported. A known driver bug around long compute → RT mode switches is tracked in Known Issues.\nIntel — compute and graphics work; RT is driver-dependent.\nlavapipe — used for CI. Compute and graphics work; RT is software-emulated, slow but functional.\nMoltenVK (macOS) — compute works; graphics and RT depend on the MoltenVK feature level.","title":"Cross-platform notes"},{"category":"section","location":"installation/#Installation","page":"Installation","text":"","title":"Installation"},{"category":"section","location":"installation/#Julia-package","page":"Installation","text":"Lava is not yet in the General registry. Install directly from the repository:\n\nimport Pkg\nPkg.add(url=\"https://github.com/SimonDanisch/Lava.jl\")\n\nOnce tagged it will be installable via Pkg.add(\"Lava\").","title":"Julia package"},{"category":"section","location":"installation/#System-requirements","page":"Installation","text":"Julia 1.12 or newer (Lava itself supports 1.11, but the required Raycore dep is gated on 1.12)\nA Vulkan 1.2+ driver with the bufferDeviceAddress and variablePointers features. Any recent NVIDIA, AMD, Intel, MoltenVK (macOS), or lavapipe ≥ 24.x driver qualifies.\nFor ray tracing: a driver exposing VK_KHR_ray_tracing_pipeline and VK_KHR_acceleration_structure. Software RT works via lavapipe's emulation, but RADV / NVIDIA give real hardware BVH traversal.\nFor graphics: an X11 or Wayland display server, and GLFW build dependencies — on Debian-likes:\napt-get install xorg-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libxext-dev mesa-utils\n\nHeadless servers without DISPLAY can run Lava under xvfb-run:\n\nDISPLAY=:0 xvfb-run -s '-screen 0 1024x768x24' julia --project ...\n\nThis is exactly how the CI runs the test suite.","title":"System requirements"},{"category":"section","location":"installation/#Verify-the-install","page":"Installation","text":"using Lava\n\nctx = Lava.vk_context()\n@show ctx.device_name\n@show ctx.rt_pipeline_properties !== nothing\n\nA successful run prints the chosen GPU and whether hardware ray tracing is available. Anything else (driver missing, surface-extension errors, validation messages) points to a system-level issue — see Debugging.","title":"Verify the install"},{"category":"section","location":"installation/#Picking-a-device","page":"Installation","text":"If multiple Vulkan devices are present, Lava picks the first discrete GPU by default. To override:\n\nENV[\"LAVA_FORCE_DEVICE\"] = \"lavapipe\"   # substring match against device name\nusing Lava\n\nCommon values: \"NVIDIA\", \"RADV\", \"Intel\", \"llvmpipe\", \"lavapipe\", \"MoltenVK\".","title":"Picking a device"},{"category":"section","location":"graphics/#Graphics","page":"Graphics","text":"Vertex, fragment, geometry, and tessellation shaders are written as plain Julia functions and compiled to SPIR-V. There is no GLSL, no shader file format, no external preprocessor — your shaders live in the same module as your CPU code, share types and helpers with the rest of your codebase, and are subject to the same Julia tooling.\n\nnote: API stability\nThe graphics surface area is functional and exercised by real-world renderers, but the public API is still being refined. Expect minor breaking changes between 0.x releases.","title":"Graphics"},{"category":"section","location":"graphics/#Pipelines","page":"Graphics","text":"The high-level entry points are Rasterizer, TrianglePipeline, and LinePipeline. Each takes a vertex and fragment shader plus optional state (blend mode, cull face, depth test, topology):\n\nusing Lava, GeometryBasics\n\nvertex(pos, mvp) = mvp * pos                       # → vec4 clip-space position\nfragment(color)  = color                            # → vec4 RGBA\n\npipeline = Rasterizer(\n    vertex   = vertex,\n    fragment = fragment,\n    blend    = :alpha,\n    cull     = :back,\n    depth    = :less,\n    topology = :triangle_list,\n)\n\nThe shader functions are regular Julia: they can call each other, dispatch on types, use packages (GeometryBasics, LinearAlgebra, StaticArrays, …), and are compiled per-instantiation just like compute kernels.","title":"Pipelines"},{"category":"section","location":"graphics/#Render-targets-and-present","page":"Graphics","text":"window = RenderWindow(width=1280, height=720, title=\"demo\")\nframebuffer = window.framebuffer\n\nwhile window_should_close(window) == false\n    draw!(pipeline, framebuffer; vertices=mesh, uniforms=params)\n    present_frame!(window)\nend\n\nOffscreen rendering uses LavaFramebuffer(width, height) directly without opening a window:\n\nfb = LavaFramebuffer(1920, 1080)\ndraw!(pipeline, fb; vertices=mesh, uniforms=params)\nimg = Array(fb)   # → Matrix{RGBA{N0f8}}","title":"Render targets and present"},{"category":"section","location":"graphics/#Textures-and-samplers","page":"Graphics","text":"img = LavaTexture2D(load(\"brick.png\"))\nsampler = LavaSampler(; filter=:linear, wrap=:clamp_to_edge)\n\nfragment(uv, tex, smp) = sample(tex, smp, uv)\n\nTextures and samplers participate in the same BDA arg-packing as buffer arguments; descriptor set management is hidden from the user.","title":"Textures and samplers"},{"category":"section","location":"graphics/#Geometry-and-tessellation","page":"Graphics","text":"Geometry and tessellation control / evaluation stages are wired in the same way: you pass geometry=... and tess_control=... / tess_eval=... to Rasterizer. The custom emitter handles all five graphics stages — Lava does not delegate to llc, which still crashes on geometry shaders today.","title":"Geometry and tessellation"},{"category":"section","location":"graphics/#Pixel-readback","page":"Graphics","text":"For most workflows you'll just call Array(framebuffer) to grab the final image after vk_flush!. The conversion handles the GPU → CPU staging, pixel format, and row-pitch unswizzling.","title":"Pixel readback"},{"category":"section","location":"#Lava.jl","page":"Home","text":"A Julia GPU backend that compiles Julia code to SPIR-V, targeting Vulkan.\n\nLava is three backends in one package, all sharing a single buffer model:\n\nCompute — full GPUArrays.jl and KernelAbstractions.jl interface. Set device = LavaBackend() and any KA-based GPU code runs on Vulkan.\nGraphics — vertex, fragment, geometry, and tessellation shaders written in plain Julia (not GLSL), driven through high-level pipelines and RenderWindows.\nRay tracing — VK_KHR_ray_tracing_pipeline with raygen / closest-hit / any-hit / miss / intersection shaders as Julia functions and a Julia-side TLAS that supports incremental updates.\n\nA single LavaArray{T,N} is usable across all stages: compute kernels can fill geometry, vertex shaders can render it, ray tracing shaders can trace against it — no copies, no format conversion.","title":"Lava.jl"},{"category":"section","location":"#Where-to-start","page":"Home","text":"If you want to… Go to\nInstall and verify your setup Installation\nUse LavaBackend() like CUDABackend() / ROCBackend() Compute\nWrite vertex/fragment shaders in Julia Graphics\nTrace rays against a TLAS Ray Tracing\nTune dispatch latency, memory transfer, scheduling Performance\nUnderstand the LLVM-IR → SPIR-V pipeline Architecture\nDiagnose a device-lost / validation error Debugging\nCheck what's known broken Known Issues\nLook up a specific function API Reference","title":"Where to start"},{"category":"section","location":"#Status","page":"Home","text":"The KernelAbstractions / GPUArrays compute path is feature-complete and well-tested (~99.7 % of the GPUArrays test suite passes). The graphics and ray tracing paths are functional and stable enough that the Hikari volumetric path tracer renders end-to-end through both, but their public APIs are still evolving — expect minor breaking changes between 0.x releases.\n\nPerformance figures on AMD RX 7900 XTX (vs AMDGPU.jl and pbrt-v4) live in the README and the Performance page.","title":"Status"},{"category":"section","location":"#Hardware","page":"Home","text":"Tested on:\n\nAMD RX 7900 XTX (Vulkan 1.4, RADV)\nAMD Radeon 8060S (Vulkan 1.4, RADV)\nNVIDIA RTX 3070 Laptop (Vulkan 1.3)\nlavapipe (software, used for CI)\n\nRequires Vulkan 1.2+ with bufferDeviceAddress and variablePointers. macOS is supported via MoltenVK.","title":"Hardware"},{"category":"section","location":"raytracing/#Ray-Tracing","page":"Ray Tracing","text":"Lava exposes VK_KHR_ray_tracing_pipeline with all stages — raygen, closest-hit, any-hit, miss, intersection — implemented as Julia functions. The TLAS and BLAS lifecycle is managed by HWTLAS, which is the recommended entry point for production code.\n\nnote: API stability\nThe ray-tracing surface area is functional and exercised by Hikari, but the public API is still being refined. Expect minor breaking changes between 0.x releases.","title":"Ray Tracing"},{"category":"section","location":"raytracing/#HWTLAS-—-recommended-high-level-entry","page":"Ray Tracing","text":"HWTLAS owns the Vulkan TLAS + BLAS lifecycle, supports incremental geometry updates via push! / delete! / update_transform!, and implements the Raycore.AbstractAccel contract so the same client code works against software and hardware backends.\n\nusing Lava, GeometryBasics, StaticArrays, LinearAlgebra\nusing Raycore: RTRay, RTHitResult\n\nhwtlas = HWTLAS(LavaBackend())\n\n# Add a mesh instance at the identity transform\npush!(hwtlas, mesh, SMatrix{4,4,Float32}(I); instance_id=UInt32(1))\n\n# After staging changes, sync to the device\nRaycore.sync!(hwtlas)\n\n# Build a ray buffer and a hit-result buffer\nrays = LavaArray([RTRay(0, 0, 5,    # origin\n                        0,\n                        0, 0, -1,   # direction\n                        1f3)])      # t_max\nhits = LavaArray(fill(RTHitResult(0,0,0,0,0,0,0,0), 1))\n\ntrace_closest_hits!(hits, rays, hwtlas.hw_accel, 1)\n\nHWTLAS exposes tlas.hw_accel::HardwareAccel, which is the lower-level handle for direct pipeline/SBT control if you need it. Most users do not.","title":"HWTLAS — recommended high-level entry"},{"category":"section","location":"raytracing/#Incremental-updates","page":"Ray Tracing","text":"Modifying geometry between frames does not rebuild the whole acceleration structure — only the dirty BLAS and a TLAS refit:\n\n# Swap one instance's mesh (e.g. an animated character)\ndelete!(hwtlas, instance_id)\npush!(hwtlas, new_mesh, transform; instance_id=instance_id)\n\n# Move an instance without rebuilding its BLAS\nupdate_transform!(hwtlas, instance_id, new_transform)\n\nRaycore.sync!(hwtlas)\n\nsync! is nonblocking: it submits the build commands on the dedicated queue and returns. The next trace_closest_hits! synchronises automatically before tracing.","title":"Incremental updates"},{"category":"section","location":"raytracing/#Writing-RT-shaders","page":"Ray Tracing","text":"Julia-side RT shaders look like regular kernels but use the RT intrinsics from Lava.rt_intrinsics. Pipelines are constructed by name:\n\nfunction my_raygen(image, accel, camera)\n    px, py = lava_launch_id_2d()\n    ray = primary_ray(camera, px, py)\n    payload = lava_rt_trace_ray(accel, ray)\n    image[px, py] = payload.color\nend\n\nfunction my_closesthit()\n    t = lava_ray_query_get_t(true)\n    prim = lava_ray_query_get_primitive_index(true)\n    lava_rt_payload_store_f32(t, 0)\nend\n\nfunction my_miss()\n    lava_rt_payload_store_f32(0, 0)\nend\n\npipeline = RayTracingPipeline(\n    raygen       = my_raygen,\n    closest_hit  = my_closesthit,\n    miss         = my_miss,\n)\n\nRayTracingPipeline builds the SBT, manages the pipeline cache, and dispatches via vkCmdTraceRaysKHR.","title":"Writing RT shaders"},{"category":"section","location":"raytracing/#Inline-ray-queries","page":"Ray Tracing","text":"For simpler workloads — most volumetric integrators, AO, shadow rays — VK_KHR_ray_query is often a better fit than a full RT pipeline. Inline ray queries run inside compute shaders and have lower setup overhead:\n\n@kernel function shadow_kernel!(visibility, origins, dirs, accel)\n    i = @index(Global)\n    o = origins[i]; d = dirs[i]\n    lava_ray_query_init(accel, UInt32(0), UInt32(0xFF),\n                        o[1], o[2], o[3], 0f0,\n                        d[1], d[2], d[3], 1f3)\n    while lava_ray_query_proceed()\n        # opaque triangles auto-commit\n    end\n    visibility[i] = lava_ray_query_get_type(true) == UInt32(1) ? 0f0 : 1f0\nend\n\nThis is the path Hikari uses for its shadow rays.","title":"Inline ray queries"},{"category":"section","location":"raytracing/#Hikari-and-Raycore-integration","page":"Ray Tracing","text":"HWTLAS implements Raycore.AbstractAccel, so any renderer that talks to Raycore (notably Hikari) gets hardware ray tracing for free by passing hw_accel=true to its integrators. The same scene file runs through Hikari's software BVH for ground-truth correctness checks.","title":"Hikari and Raycore integration"}]
}
